apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: backend
  name: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  strategy: {}
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - image: datdiep/pomodoro-backend
        name: backend
        ports:
        - containerPort: 80
        resources: {}
        imagePullPolicy: Never
status: {}
